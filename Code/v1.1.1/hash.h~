#include "structure.h"
#include "signature.h"
#include "index.h"

Hasht* hash_initialize();

// add a new map in the Hashtable
// if a Map with the same signature already exist in the Hashtable, nothing is added 
void hash_insert(Hasht* h, Map *M);

